{   % while do
    
    var num : integer
    
    num := 8
    
    while (num > -2)
    do {
        num := num - 1
        }

    if num = -2
    then write "passed", newline
    else write "failed", newline
}