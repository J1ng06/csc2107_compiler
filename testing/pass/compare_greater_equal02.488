{   %comparator GREATER THAN (variable)

    var num : integer
    var num2 : integer
    
    num := 5
    num2 := 6
    
    if num2 >= num + 1
    then write "passed", newline
    else write "failed", newline
    
    if num >= num2
    then write "failed", newline
    else write "passed", newline
}
